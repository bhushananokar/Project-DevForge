name: keda_queue_depth
description: "Report KEDA-backed task queue depth per role (read-only)."
version: "1.0.0"
side_effect_level: read-only
permissions: []
input_schema:
  type: object
  properties:
    role:
      type: string
      description: "Worker role / stream name"
  required: [role]
  additionalProperties: false
output_schema:
  type: object
timeout: 30.0
retry:
  max_attempts: 1
